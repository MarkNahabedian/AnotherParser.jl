TODO:

Use SubString for values of CharacterLiteral, StringLiteral and
RegexNode.

How can bnf_str pass __source__ to the DerivationRule constructor?
More complicated context argument?  There are DerivationRule
construtor methods that take source as their third argument.

DerivationRule recognize method should note start index so it can pass
it and the end index as additional arguments to the constructor
function so that the entire matched input is available to the
constructor.  With this, we will no longer need StringCollector.

syntactic sugar for BNF by transforming Julia Exprs:

str macro for ref.  Need context to get dictionary

::= for non-terminal definitions

&& and || for Sequence and Alternatives respectively.

What about Constructor?
Just use function call syntax?


