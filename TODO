TODO:

If we get source locations working for BNF nodes then maybe we can use
the source location rather than the result of next_node_id as the node
unique id.  This would ensure that the node ids are stable so long as
the sources haven't changes.


Maybe have a separate file for each subtype of BNFNode.  We should at
least move the walk_nodes functions to nearer their node definitions.
How to preserve git history across such a move?

For safety, do this in a branch to make sure it works.

* Use cp to make one new copy of BNFtypes.jl for each node type.
    subtypes(BNFNode)
    15-element Vector{Any}:
     Alternatives
     BNFRef
     CharacterInSet
     CharacterLiteral
     CharacterSatisfiesPredicate
     Constructor
     DerivationRule
     Empty
     EndFenced
     EndOfInput
     Excluding
     RegexNode
     Repeat
     Sequence
     StringLiteral
* git add and commit those copies unmodified.
* remove the duplicate ocde
* commit

See git diff -C flag.


Rationalize and simplify DEBUG_BNFNODES and enable_debug_logging_for
keyarg.


Merge debug_parsing functionality into parse or recognize1.


When constructing BNFNode names, don't include package prefix.


A way to have recognize report at what index it failed to match what
expression.


Do we want a BNFNode type that's RepeatWithSeparator, or add a
separator::BNFNode keyarg to Repeat.


Make sure method signatures of debug_parsing correspond with those of
recognize and recognize1.  Or maybe add a debug keyarg to the regular
functions.  This is possibly mooted by the
recognize/recognize1/debug_parsing cleanup suggested above.


We need an easier way to write constructor functions.


How can bnf_str pass __source__ to the DerivationRule constructor?
More complicated context argument?  There are DerivationRule
construtor methods that take source as their third argument.


syntactic sugar for BNF by transforming Julia Exprs:


Maybe have a str macro for BNFRef.

